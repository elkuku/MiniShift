{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center">
        <h1 class="text-danger">{{ status_code }}</h1>
        <img src="{{ asset('bundles/app/img/sad-monkey.png') }}"/>
        <p class="lead text-center">{{ status_text }}</p>
        <code>=;(</code>
    </div>
{% endblock %}

